// get first docvar doc = fl.documents[0];// get first stagevar stage = doc.timelines[0]; // get first layervar l = stage.layers[0];// get first framevar f = l.frames[0];// get libraryvar lib = doc.library;fl.outputPanel.clear();for (var n in f.elements) {		var e = f.elements[n];	fl.outputPanel.trace(n+": "+e.elementType);	if (e &amp;&amp; e.elementType == "instance") {		e.selected = true;	}}