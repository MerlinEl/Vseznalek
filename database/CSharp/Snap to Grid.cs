        public void SnapToGrid(Control control) {            //double modX = (control.Left - Location.X) % GridSize;            //double modY = (control.Top - Location.Y) % GridSize;            //if (modX > GridSize / 2) {            //    control.Left = (int)(control.Left + (GridSize - modX));            //} else {            //    control.Left = (int)(control.Left - modX);            //}            //if (modY > GridSize / 2) {            //    control.Top = (int)(control.Top + (GridSize - modY));            //} else {            //    control.Top = (int)(control.Top - modY);            //}                        double x = Math.Round((double)control.Left / GridSize) * GridSize;            double y = Math.Round((double)control.Top / GridSize) * GridSize;            control.Left = (int)(x);            control.Top = (int)(y);            Console.WriteLine(Name + " > SnapToGrid >  Pos:{0}", control.Location);        }