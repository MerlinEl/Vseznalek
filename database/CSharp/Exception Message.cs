            if (!File.Exists(srcPath)) throw new FileNotFoundException();            if (!Directory.Exists(trgtDir)) throw new DirectoryNotFoundException();//EXAM        internal static void CopyReplaceFile(string sourceFpath, string targetFpath, bool createTargetDir) {            if (!File.Exists(sourceFpath)) throw new FileNotFoundException();            var trgtDir = Path.GetDirectoryName(targetFpath);            if (!Directory.Exists(trgtDir)) {                if (createTargetDir) {                    Directory.CreateDirectory(trgtDir);                } else {                    throw new DirectoryNotFoundException();                }            }            File.Copy(sourceFpath, targetFpath, true);        }//OR            if (String.IsNullOrEmpty(srcPath) || string.IsNullOrEmpty(trgtDir)) {                throw new ArgumentException("File or Directory not specified.");            }            if (!File.Exists(srcPath) || !Directory.Exists(trgtDir)) {                throw new ArgumentException("File or Directory not found.");            }