				GraphicsPathIterator pathIterator = new GraphicsPathIterator(SplinePath);				string str = "Total point = " + pathIterator.SubpathCount.ToString();				// Read all subpaths and their properties  				for (int i = 0; i < pathIterator.SubpathCount; i++) {					int strIdx, endIdx;					bool bClosedCurve;					pathIterator.NextSubpath(out strIdx,					   out endIdx, out bClosedCurve);					str = "Start Index = " + strIdx.ToString()								+ " , End Index = " + endIdx.ToString()								+ ", IsColose = " + bClosedCurve.ToString();					Console.WriteLine(str);				}