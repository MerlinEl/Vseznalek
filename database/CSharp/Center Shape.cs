                // Center the Shape to ClientRectangle                //if (pointsCount == 3) {                    //var sideLength = GetSideLength(shape[0], shape[1]);                    //var shapeHeight = sideLength * Math.Sqrt(3.0) / 2;                    //var offsetY = (Height - shapeHeight) / 2;                    //for (var i = 0; i < shape.Length; i++) {                    //    shape[i].Y += (int)offsetY;                    //}                //} else {                    var angleAlpha = (360 / pointsCount).DegToRadians();                    var shapeHeight2 = r * (1 + Math.Cos(angleAlpha / 2));                    var offsetY2 = (Height - shapeHeight2) / 2;                    for (var i = 0; i < shape.Length; i++) {                        shape[i].Y += (int)offsetY2;                    }                //}        private double GetSideLength(PointF p1, PointF p2) {                       return Math.Sqrt(Math.Pow((p2.X - p1.X), 2) + Math.Pow((p2.Y - p1.Y), 2));        }