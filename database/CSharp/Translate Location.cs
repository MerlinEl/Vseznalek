//The following will give you mouse coordinates relative to your control. For example, this results in (0,0)// if mouse is over top left corner of the control:var coordinates = yourControl.PointToClient(Cursor.Position);//Form1_MouseDown// Calculate the startPoint by using the PointToScreen  startPoint = control.PointToScreen(new Point(e.X, e.Y));//Form1_MouseMove// Calculate the endpoint and dimensions for the new  // rectangle, again using the PointToScreen method.Point endPoint = ((Control) sender).PointToScreen(new Point(e.X, e.Y));int width = endPoint.X-startPoint.X;int height = endPoint.Y-startPoint.Y;theRectangle = new Rectangle(startPoint.X, startPoint.Y, width, height);