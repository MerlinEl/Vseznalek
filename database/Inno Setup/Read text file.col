                  if LoadStringsFromFile(AppXML, ArrStr) then                    begin                     IDLine := ArrStr[2];                     ArrStr[2] := '  <id>Walker_'+AppID+'</id>';                     MsgBox('ID line:' + IDLine, mbInformation, MB_OK);                     SaveStringsToFile(const FileName: String; const S: TArrayOfString; const Append: Boolean): Boolean;                    //for I := 0 to GetArrayLength(ArrStr)-1 do                     // S := S + ArrStr[I];                      //MsgBox('List of values:' + S, mbInformation, MB_OK);                      //MsgBox('Read app:' + ExpandConstant(ArrStr), mbInformation, MB_OK);end