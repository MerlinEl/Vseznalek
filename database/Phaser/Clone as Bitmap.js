    cloneAsBitmap() {        // create a texture on scene with same size and position        const texture = this.scene.add.renderTexture(this.x - this.width / 2, this.y - this.height / 2 - 200, this.width, this.height);        // draw all components on it        [this.bg_skin_01, this.flicker_01, this.tf_01].forEach((c) => {            texture.draw(c, c.width / 2, c.height / 2);        });        //texture.setTintFill("0xFF0000");        //texture.alpha = 0.5;        //ground.setDisplaySize(1200,100)        // debug        var g = this.scene.add.graphics().setVisible(false);        g.lineStyle(2, 0xff0000, 1);        g.strokeRect(0, 0, this.width, this.height);        texture.draw(g);        // move texture under container        texture.depth = this.depth;        return texture;    }