		scene.make.graphics({			add: false,			fillStyle: { color: 0xFFFFFF },			lineStyle: { width: 4, color: 0xa9a9a9 }		})			.fillCircle(24, 24, 20)			.strokeCircle(24, 24, 20)			.generateTexture('pcircle_01', 48, 48)			.destroy();		scene.add.image(400, 300, 'pcircle_01');//.setScale(8).setAlpha(0.8);		this.particles = scene.add.particles('pcircle_01');//.setDepth(this.sun_anim.depth + 1);// OR		var graphics = scene.add.graphics();		graphics.fillStyle(0x800000, 1).fillCircle(60, 60, 50);// OR Advanced		var th = 4; // thickness		var ra = 20; // radius		var of = 8; // border offset		scene.make.graphics({			add: false,			fillStyle: { color: 0xFFFFFF },			lineStyle: { width: th, color: 0xa9a9a9 }		})			.fillCircle(ra + th + of, ra + th + of, ra)			.strokeCircle(ra + th + of, ra + th + of, ra + of)			.generateTexture('pcircle_01', (ra + th + + of) * 2, (ra + th + + of) * 2)			.destroy();