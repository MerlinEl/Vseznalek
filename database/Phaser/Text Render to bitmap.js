//https://labs.phaser.io/edit.html?src=src/game%20objects/render%20texture/resize%20rendertexture.js&v=3.70.0document.body.style = 'margin:0;';var config = {    type: Phaser.AUTO,    width: 536,    height: 183,    scene: {        create    },    banner: false}; function create () {        let graphics = this.make.graphics();    graphics.lineStyle(1,0xff0000);    graphics.fillStyle(0x02455f, .5);    graphics.strokeRect(10, 20, 160, 80);    graphics.fillRect(10, 20, 160, 80);        let helloWorld = this.add.text( 35, 50, "Hello World")      .setFont("20px Arial")      .setColor('#ffffff');        let renderTexture = this.add.renderTexture(9, 19, 172, 102);        renderTexture.draw(graphics);    renderTexture.draw(helloWorld);        renderTexture.saveTexture('newTexture');        graphics.destroy();    helloWorld.destroy();    renderTexture.destroy();        let image = this.add.image(100,100, 'newTexture').setOrigin(.5);         this.tweens.add({        targets: image,        angle: {from: 0, to: 360},        duration: 6000,        repeat:-1    });        }new Phaser.Game(config);