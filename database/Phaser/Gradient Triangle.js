class Example extends Phaser.Scene{    create ()    {        const graphics = this.add.graphics();        var offset = {x:300, y:200};        var color_inner = 0xff0000;        var color_outer = 0x00ff00;        graphics.fillGradientStyle(color_inner, color_outer, color_outer, color_inner, 1);        graphics.fillTriangle(            50+offset.x, 10+offset.y,             10+offset.x, 100+offset.y,             90+offset.x, 100+offset.y        );    }}const config = {    width: 800,    height: 600,    type: Phaser.WEBGL,    parent: 'phaser-example',    scene: Example};const game = new Phaser.Game(config);