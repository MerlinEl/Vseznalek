//https://labs.phaser.io/edit.htmclass Example extends Phaser.Scene{    rect = null;    create() {        var colorsTable = [            0xFF0000,            0x00FF00,            0x0000FF,            0xFF00FF,            0xFFFF00,            0x00FFFF        ];        var g = this.add.graphics({x: 400, y:300});        g.fillStyle(colorsTable[Math.floor(Math.random() * 6)], 1.0);        g.beginPath().fillRect(-50, -50, 100, 100).closePath().fillPath();        g.rotation = Math.random();        this.rect = g;    }    update() { if (this.rect)  this.rect.rotation += 0.01; }}const config = {    type: Phaser.WEBGL,    parent: 'phaser-example',    width: 800,    height: 600,    scene: Example};const game = new Phaser.Game(config);