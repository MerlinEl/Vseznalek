        console.log("SimpleCage > hilightFlicker > cage:", this, "type:", type);        var flicker = this.bg_skin_02;        var clr = type == "good" ? "0x00FF00" : "0xFF0000";        flicker.setTintFill(clr);        this.scene.add.tween({            targets: [flicker],            alpha: { start: 0, to: 0.8 },            ease: "Sine.easeInOut", //Linear            duration: 300,            repeat: 1,            yoyo: true,            onUpdate: function (tween, target, key, current, previous, param) {                const value = Math.floor(tween.getValue());                console.log("color:", clr, "target:", target, "tween:", tween, "current:", current);                // const colorObject = Phaser.Display.Color.Interpolate.ColorWithColor(                // 					Phaser.Display.Color.ValueToColor('#000000'),                // 					Phaser.Display.Color.ValueToColor('#FF0000'),                // 					100,                // 					value                // 				)                // const color = Phaser.Display.Color.RGBToString(colorObject.r, colorObject.g, colorObject.b)                // max_suspicion.setColor(color);            },        });        // var colorBlend = { step: 0 };        // var flicker = this.bg_skin_02;        // var startColor = this.bgColor;        // var endColor = "good" ? "lime" : "red";        // this.scene.add        //     .tween(colorBlend)        //     .to({ step: 100 }, 2000, Phaser.Easing.Default, false)        //     .onUpdateCallback(() => {        //         flicker.tint = Phaser.Color.interpolateColor(startColor, endColor, 100, colorBlend.step, 1);        //     });