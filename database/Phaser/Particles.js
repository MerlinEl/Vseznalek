		var weightedCircle = {			getRandomPoint: function (vec){				var t = Phaser.Math.PI2 * Math.random(); // angle in radians > 6.28318(360°) * random 0.0 - 1.0				var r = Math.pow(Math.random(), -0.1); // random number > 1				// vec.x = this.circle.x + r * Math.cos(t) * this.circle.radius;				// vec.y = this.circle.y + r * Math.sin(t) * this.circle.radius;				var circle_radius = 30;				vec.x = r * Math.cos(t) * circle_radius; // center is always 0				vec.y = r * Math.sin(t) * circle_radius; // center is always 0				return vec;			}		};// move particles to front		var star_particle = this.scene.add.particles('sun_stars').setDepth(this.sun_bg.depth+1);		this.star_emitter = star_particle.createEmitter({			frame:["star_01", "star_02", "star_03", "star_04"],			x: this.x, y: this.y,			lifespan: 1000,			frequency: 10,			radial: { min: -200, max: 200 },			// gravityX: -200,			// quantity: 4,			maxVelocityX: 20,			maxVelocityY: 20,			speedY: { min: -200, max: 200 },            speedX: { min: -200, max: 200 },			scale: { start: 0.2, end:1.4 },			alpha: { start: 1, end:0 },			blendMode: Phaser.BlendModes.HARD_LIGHT,			emitZone: { type: 'random', source: weightedCircle }		})		var star_particle = this.scene.add.particles('sun_stars');		this.star_emitter = star_particle.createEmitter({			x: this.x, y: this.y,			frequency: 100,			// speed: { min: -80, max: 80 },			speedY: { min: -100, max: 100 },            speedX: { min: -100, max: 100 },			// angle:45,			scale: { start: 0.2, end:1.4 },			alpha: { start: 1, end:0 },			rotate: { start: -360, end:360 },			frame:["star_01", "star_02", "star_03", "star_04"],			minParticles: 8,			maxParticles: 20,			blendMode: Phaser.BlendModes.HARD_LIGHT,			lifespan: 1000,			// gravityY: 80,			// tint: 0xFFFF99,			// active: false,			//on:false		});		//this.star_emitter.tint.onChange(0xFFFF99);		//this.star_emitter.manager.destroy();		//this.star_emitter.manager.emitters.remove(star_emitter);		/*var shape2 = new Phaser.Geom.Circle(0, 0, 60);			var star_particle = this.scene.add.particles('sun_stars').setDepth(this.sun_bg.depth+1);		this.star_emitter = star_particle.createEmitter({			frame:["star_01", "star_02", "star_03", "star_04"],			x: this.x, y: this.y,			speedY: { min: -100, max: 100 },            speedX: { min: -100, max: 100 },			frequency: 100,			lifespan: 1000,			quantity: 1,			scale: { start: 0.4, end: 1.2 },			rotate: { start: -360, end:360 },			alpha: { start: 0, end:1 },			minParticles: 8,			maxParticles: 20,			blendMode: Phaser.BlendModes.HARD_LIGHT,			//emitZone: { type: 'random', source: shape2, quantity: 20, yoyo: false }		})*/	createStarParticles(){		var star_particle = this.scene.add.particles('sun_stars').setDepth(this.sun_bg.depth+1);		this.star_emitter = star_particle.createEmitter({			x: this.x, y: this.y,			frequency: 100,			// speed: { min: -80, max: 80 },			speedY: { min: -100, max: 100 },            speedX: { min: -100, max: 100 },			// angle:45,			scale: { start: 0.2, end:.8 },			alpha: { start: 0, end:1 },			rotate: { start: -360, end:360 },			frame:["star_01", "star_02", "star_03", "star_04"],			minParticles: 8,			maxParticles: 20,			blendMode: Phaser.BlendModes.SOFT_LIGHT,			lifespan: 1000,			on:false		});	}