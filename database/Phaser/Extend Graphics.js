class Unit extends Phaser.GameObjects.Graphics {    constructor(scene, x, y, stats){      super(scene, x, y)      this.stats = stats      this.drawSelf()	   this.hitpoly = new Phaser.Geom.Polygon(this.points)	   this.setInteractive(this.hitpoly, Phaser.Geom.Polygon.Contains)     this.hitpoly.on.("pointerdown", () => {this.onClick() })    }    drawSelf() {      this.lineStyle(2, 0xCC0000, 1.0);      this.fillStyle(0x888888, 1.0);      this.moveTo(50, 0)      this.lineTo(100, 100)      this.lineTo(50, 65)      this.lineTo(0,100)      this.closePath()      this.stroke()      this.fill()      this.scaleX = 0.5      this.scaleY = 0.5    }    onClick(){      console.log("whatup")    }  }