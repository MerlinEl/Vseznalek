//https://codepen.io/DavidTalevski/pen/bGJJvRyclass Example extends Phaser.Scene {    preload() {        this.load.image('bomb', 'https://upload.wikimedia.org/wikipedia/commons/thumb/e/e7/Phaser_Logo.png/1200px-Phaser_Logo.png');    }    create() {        const scale = 0.2;        const bomb1 = this.add.sprite(400, 300, 'bomb');        bomb1.setScale(scale)        bomb1.setOrigin(0, 0);        bomb1.preFX.addGlow(0xffff00, 4, 0, false, 0.1, 32);        requestAnimationFrame(() => {            // This draws the image correctly, but the texture is as big as the screen which is not good            const texture1 = this.textures.addDynamicTexture("bomb_glow1", 800, 600);            texture1.draw(bomb1, 0, 0);            // This is the expected usage and creates a texture as big as the image            const texture2 = this.textures.addDynamicTexture("bomb_glow2", bomb1.width * scale, bomb1.height * scale);            texture2.draw(bomb1, 0, 0);            this.add.sprite(400, 300, "bomb_glow1")            this.add.sprite(400, 300, "bomb_glow2")            bomb1.destroy();        });    }}const config = {    type: Phaser.AUTO,    width: 800,    height: 600,    backgroundColor: '#2d2d2d',    parent: 'phaser-example',    scene: Example};const game = new Phaser.Game(config);