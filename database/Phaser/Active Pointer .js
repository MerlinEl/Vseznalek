			var  rect= new Phaser.Geom.Rectangle(0, 0, 20, 20);            let graphics = scene.add.graphics({                 x:dot.x + 10, y:dot.y + 10,                 fillStyle: {color: 0x00ff00 }             });            let pointer = scene.input.activePointer;            graphics.fillRectShape(rect);//.setPosition(dot.x + 10, dot.y + 10);            graphics.setInteractive(rect, () => {                if(pointer.isDown) {                    console.log('clicked');                } else if (pointer.isReleased){                   console.log('leaved');                     }            });