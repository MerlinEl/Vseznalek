// this is generated code - copy what you need 😎import Phaser from 'phaser'import WebFontFile from './WebFontFile'class TextStyleScene extends Phaser.Scene{	preload()	{		const fonts = new WebFontFile(this.load, 'Quicksand')		this.load.addFile(fonts)	}	create()	{		const x = this.scale.width * 0.5		const y = this.scale.height * 0.5		this.add.text(x, y, 'Hello, World!', {			fontFamily: 'Quicksand',			fontSize: '48px',			color: '#fff',			fontStyle: 'normal'		})	}} // more info: https://blog.ourcade.co/posts/2020/phaser-3-google-fonts-webfontloader/import Phaser from 'phaser'import WebFontLoader from 'webfontloader'export default class WebFontFile extends Phaser.Loader.File{	/**	 * @param {Phaser.Loader.LoaderPlugin} loader	 * @param {string | string[]} fontNames	 * @param {string} [service]	 */	constructor(loader, fontNames, service = 'google')	{		super(loader, {			type: 'webfont',			key: fontNames.toString()		})		this.fontNames = Array.isArray(fontNames) ? fontNames : [fontNames]		this.service = service	}	load()	{		const config = {			active: () => {				this.loader.nextFile(this, true)			}		}		switch (this.service)		{			case 'google':				config['google'] = {					families: this.fontNames				}				break			default:				throw new Error('Unsupported font service')		}				WebFontLoader.load(config)	}}