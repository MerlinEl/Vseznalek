// Disable menu or Mouse-RC        document.addEventListener(            "contextmenu",            function (e) {                e.preventDefault(); // Prevent the default context menu                // Orien.log("Disabled menu on Mouse-RC > clicked location:", e.pageX, e.pageY);                //this.fillArea(new Point2D(e.pageX, e.pageY));            }.bind(this)        );        // distinguish between left and right mouse click        document.addEventListener(            "mousedown",            function (e) {                // skip if graphics mouse buttons clicked                // prettier-ignore                switch(e.button){                    case 0: this.mouseButtonType = "left"; break;                    case 1: this.mouseButtonType = "middle"; break;                    case 2: this.mouseButtonType = "right"; break;                }                Orien.log("Mouse clicked:", this.mouseButtonType, "btn:", e.target);                this.updateMouseButtons();            }.bind(this)        );        // Disable menu or Mouse-RC        scene.input.mouse.disableContextMenu();        // Distinguish between left and right mouse click        scene.input.on(            "pointerdown",            function (pointer, gameobject) {                if (pointer.rightButtonDown()) {                    // RIGHT CLICK                    this.mouseButtonType = "left";                    // if (pointer.getDuration() > 500) { } else { }                } else {                    // LEFT CLICK                    this.mouseButtonType = "right";                    // if (pointer.getDuration() > 500) { } else { }                }                Orien.log("Mouse clicked:", this.mouseButtonType, "gameobject:", gameobject);            },            this        );