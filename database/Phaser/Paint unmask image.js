//https://codepen.io/hoshinokoe/pen/MWyBzYbconsole.clear();document.getElementById('version')  .textContent = 'Phaser v' + Phaser.VERSION;var config = {  type: Phaser.AUTO,  width: 800,  height: 600,  loader: {    baseURL: 'https://labs.phaser.io',    crossOrigin: 'anonymous'  },  physics: {    default: 'arcade',    arcade: {      gravity: { y: 200 }    }  },  scene: {    preload: preload,    create: create,    update: update  }};var game = new Phaser.Game(config);var keys;var userCanvas;var quicksaveCanvas;var savPoint;function preload() {  this.load.image('sky', 'assets/skies/space3.png');  this.load.image('brush', 'assets/sprites/brush1.png');   this.load.image('tiles', 'assets/textures/grass.png');}function create() {  // arrow key controls  keys = {    left: this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.LEFT),    right: this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.RIGHT),  };  this.add.image(400, 300, 'sky');    // the texture we will doodle on  userCanvas = this.add.renderTexture(0, 0, 800, 600);  quicksaveCanvas = this.add.renderTexture(0, 0, 800, 600);    for (var y = 0; y < 2; y++)    {        for (var x = 0; x < 2; x++)        {            userCanvas.draw('tiles', x * 512, y * 512);        }    }  userCanvas.fill(0xff0b0b, 1, 95, 95, 10, 10);  quicksaveCanvas.visible = false;  // draw on the texure as the pointer moves  this.input.on('pointermove', function (pointer) {    if (pointer.isDown) {      userCanvas.erase('brush', pointer.x - 32, pointer.y - 32);            var textureManager = this.textures;        //userCanvas.snapshotArea(100, 100, 128, 128, function (image)        //{        //    document.body.appendChild(image);        //    if (textureManager.exists('area'))        //    {        //        textureManager.remove('area');        //    }        //    textureManager.addImage('area', image);        //});            //var pixel = userCanvas.texture.getPixel(100, 100);      //var imageData = userCanvas.context.getImageData(100, 100, 1, 1);            var gl = userCanvas.gl.canvas.getContext('experimental-webgl');      var pixels = new Uint8Array(1 * 1 * 4);      gl.readPixels(100, 100, 1, 1, gl.RGBA, gl.UNSIGNED_BYTE, pixels);      var pixel = {        r: pixels[0],        g: pixels[1],        b: pixels[2]      };      document.getElementById('color').textContent =  "Pixel(100, 100): rgb(" + pixel.r + "," + pixel.g + "," + pixel.b + ")";    }  }, this);}function update() {  // save texture for later usage  if ( Phaser.Input.Keyboard.JustDown(keys.right) ) {    console.log("saving and resetting texture...");    // save current texture    userCanvas.saveTexture('doodle');    // draw texture to hidden 'savpoint' renderTexture    quicksaveCanvas.clear();    quicksaveCanvas.draw('doodle');    quicksaveCanvas.saveTexture('savepoint');        console.log(userCanvas.texture.getPixel(100, 100));  }    // load saved texture  if ( Phaser.Input.Keyboard.JustDown(keys.left) ) {    console.log("loading texture...");        // wipe again draw the texture prevously saved    userCanvas.clear();    userCanvas.draw( 'savepoint' );       }  }