		// mask animation 		const circle = this.scene.make.graphics({add:false})		.fillStyle(0xFFFFFF, 1)		.fillCircle(0, 0, 100);		circle.setPosition(x + animset_01.x, y + animset_01.y);		const mask = circle.createGeometryMask();		animset_01.setMask(mask);		const mask_image = this.scene.make.image({ x: x + animset_01.x, y: y + animset_01.y, key: 'giude_bg_mask_01', add: false });		var mask = new Phaser.Display.Masks.BitmapMask(scene, mask_image)		// mask.invertAlpha = true;        animset_01.setMask(mask);document.body.style = 'margin:0;';var config = {    type: Phaser.CANVAS,    width: 536,    height: 183,    scene: {        create    },    banner: false}; function create () {    const container = this.add.container(0, 0);    const width = config.width;    const height = config.height;    let g = this.make.graphics({add:false});        g.fillStyle(0xffff00)    g.fillRect(10,10, 150, 150);        g.generateTexture('map', 150, 150);      const planet = this.add.sprite(50, 10, 'map')      .setOrigin(0)    const mask = this.make.graphics({add:false})      .fillStyle(1000000, 0.5)      .fillCircle(0, 0, 200);        container.add([planet]);    container.mask = new Phaser.Display.Masks.GeometryMask(this, mask);}new Phaser.Game(config);