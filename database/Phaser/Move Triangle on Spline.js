    static moveTriangleAlongSplineTest() {        // Generates an "arrow" sprite        var graphics = scene.add.graphics();        graphics.setVisible(false);        graphics.fillStyle(0x00ff00, 1);        graphics.beginPath();        graphics.moveTo(0, 0);        graphics.lineTo(30, 10);        graphics.lineTo(0, 20);        graphics.lineTo(0, 0);        graphics.fillPath();        graphics.generateTexture("sprite", 60, 40);        var path = new Phaser.Curves.Path(400, 400);        path.ellipseTo(200, 100, 0, 360, false, 0);        var follower = scene.add.follower(path, 400, 300, "sprite");        follower.depth = 10000;        follower.startFollow({            duration: 4000,            from: 0,            to: 1,            repeat: -1,            positionOnPath: true,            rotateToPath: true,        });    }