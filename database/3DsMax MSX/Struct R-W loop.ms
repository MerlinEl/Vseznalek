	sd = struct SNAP_DATA (active, markSize,hilite, snapRadius),	old_snap_settings =  ( 				sd \		snapMode.active \		snapMode.markSize \		snapMode.hilite \		snapMode.snapRadius	),	new_snap_settings = (sd "true" "40" green 20),       fn transferSettings src_obj trgt_obj props:#() = (				for p in props do  (						if not (hasProperty src_obj p) or not (hasProperty trgt_obj p) do continue			setProperty trgt_obj p (getProperty src_obj p)		)	)	fn initSnapSettings = (				transferSettings new_snap_settings snapMode props:#("active", "markSize", "hilite", "snapRadius") 	),	fn restoreSnapSettings = (				transferSettings old_snap_settings snapMode props:#("active", "markSize", "hilite", "snapRadius") 	),