fn saveSelectionLocal = (	if maxFilePath.count == 0 do (		messageBox "Scene mut be saved. Save scene first." title:"Save Selection Local:"		return OK	)		if selection.count == 0 do (				messageBox "Select object(s) which will be saved in separate MAX file." title:"Save Selection Local:"		return OK	)	--local scene_name = maxFilePath + maxFileName	local new_file_name = maxFilePath + selection[1].name + ".max"	saveNodes selection new_file_name	messageBox ("Selection are saved here:\n"+new_file_name)  title:"Save Selection Local:")saveSelectionLocal()