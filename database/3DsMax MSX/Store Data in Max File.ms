--add a Custom Attribute to it and store data in it. It can be usefull for all sorts of things. If you’re making a custom renderpresets tool, I’d store my settings there.rootNode.attributes hasOwnPropertyexampleCA = attributes example(	fn popupFn = format "Hey man!"	on load do ::popup = popupFn)custAttributes.add rootNode exampleCAtheScene = (refs.dependents rootNode)[1]show theScene --list methodsselect theScene.world.childrentheScene.Environment.Ambient_Light = color 255 128 0show theScene.RenderersetAppData rootnode 1 "#(1,2,3)"flop = getAppData rootnode 1 -- returns a string of the multi arrayglop = execute(flop)getAppData rootnode 2