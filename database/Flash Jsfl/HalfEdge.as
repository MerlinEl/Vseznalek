			var spline = sel[0];	var all_contours = spline.contours;	for (i=0;  i<all_contours.length;  i++){		// ..		var contour = all_contours[i];		var he = contour.getHalfEdge();		var iStart = he.id;		var id = 0;		while (id != iStart){		// see if the edge is linear		var edge = he.getEdge();		var vrt = he.getVertex();		var x = vrt.x;		var y = vrt.y;		fl.trace("vrt: " + x + ", " + y);		he = he.getNext();		id = he.id;		}	}