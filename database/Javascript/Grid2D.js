cell_size // velikost kartycell_index // číslo kartygap // mezeraw // šířka schránkyh // výška schránky// If all cards have same sizecolumn_count = cage.w / crad.w; // počet sloupcůgap = rest / column_count -1 // velikost mezeryrows_count =  cage.h / crad.h; // počet řádkůcard_index = 5                    5             3       == 2collumn_index = card_index % column_count                    5             3   == 1.6  == ceil(2)row_index = Math.ceil(card_index / column_count);x = (collumn_index - 1) * crad.w;y = (row_index - 1) * crad.h;*/