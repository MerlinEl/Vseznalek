    /** Check if two rectangles are overlapping      * @source https://gist.github.com/Daniel-Hug/d7984d82b58d6d2679a087d896ca3d2b    */    overlaps(r) {        // no horizontal overlap        if (this.left >= r.right || r.left >= this.right) return false;        // no vertical overlap        if (this.top >= r.bottom || r.top >= this.bottom) return false;        return true;    }// OR    overlaps(r) {        let [left1, top1, right1, bottom1] = [this.left, this.top, this.right, this.bottom],            [left2, top2, right2, bottom2] = [r.left, r.top, r.right, r.bottom];        return !(            top1 > bottom2 || // first bellow second    ◒             top2 > bottom1 || // second below first     ◒            right1 < left2 || // first before second    ◐            right2 < left1 || // second before first    ◐            bottom1 < top2 || // first under second     ◓            bottom2 < top1 || // second under first     ◓            left1 > right2 || // first after second     ◑            left2 > right1    // second after first     ◑        );    }