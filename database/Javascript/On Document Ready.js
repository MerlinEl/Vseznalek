console.log("Global Include:", this);$("document").ready(function(){		console.log( "ready!" );	var canvas = document.getElementById('canvas');	console.log("Global Include > canvas:", canvas);	$(document).on('ready', handler) binds to the ready event from jQuery. The handler is called when the DOM is loaded. Assets like images maybe still are missing. It will never be called if the document is ready at the time of binding. jQuery uses the DOMContentLoaded-Event for that, emulating it if not available.$(document).on('load', handler) is an event that will be fired once all resources are loaded from the server. Images are loaded now. While onload is a raw HTML event, ready is built by jQuery.var body = document.body;	console.log("Global Include > body:", body);		var blue_bg = new createjs.Shape();	blue_bg.graphics.beginFill("#003399").drawRect(0, 0, 1024, 768);	var preloader_slot = new createjs.MovieClip();	preloader_slot.addChild(blue_bg);	stage.addChild(preloader_slot);});window.onload = function(){		alert( "window ready!" );	console.log( "window ready!" );};function pageload(){	var e = (new Date).getTime();	var t = (e-before)/1e3;	var n = document.getElementById("loadingtime");	n.innerHTML="Page load: "+t+" seconds.";	console.log("Global Include > pageload:", n);}window.onload=function(){pageload()}let div = document.createElement("div")div.innerText = "aasfjdpfodjfopsofj"//div.innerHTML = "<div style='z-index:100000' id='owlreporter-preloader'><div id='loader'></div><div class='loader-section section-left'></div><div class='loader-section section-right'></div></div>";console.log("Global Include: div A:", div)div.append(createTileArea())//document.head.insertBefore(div, document.head.firstChild);console.log("Global Include: div B:", div)let body = document.createElement("body")body.append(div)document.head.appendChild(body);function getRandomNumber(max){	return Math.floor(Math.random() * max);}function createTileArea() {	var colors = ["Red", "Green", "Blue"];	var stage = new createjs.Stage("tileArea");	stage.name = "stage";	var size = 50;	for (row = 0; row < 10; row++) {		for (col = 0; col < 10; col++) {			var id = row + "_" + col;			var color = colors[getRandomNumber(3)];			var tile = new createjs.Shape();			tile.graphics.beginFill(color);			tile.graphics.drawRect(0, 0, size, size);			tile.graphics.endFill();			tile.x = col * size;			tile.y = row * size;			tile.height = size;			tile.width = size;			tile.name = id;			stage.addChild(tile);		}	}	stage.update();	return stage;}