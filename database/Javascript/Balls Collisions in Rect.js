this.stop();var tl = this;var speed = 3.5;var area_bounds = Orien.getRealBounds(tl, {centred:false, draw:true});var polen_bounds = Orien.getRealBounds(tl.polen_01, {centred:true, draw:true});var polen = Orien.collect(tl, ["polen"]);console.log("b:", area_bounds)// setup polenpolen.forEach(p=>{ 	p.dirX = "right";	p.dirY = "btm";});var random_pos = {		x:Orien.randomBetween(0, area_bounds.w),	y:Orien.randomBetween(0, area_bounds.h)}this.on("tick", function() {	updatePolenPosition()});function updatePolenPosition(){		polen.forEach(p=>{ 		bounceInBounds(p);	});}function bounceInBounds(p){		var pos = {x:p.x, y:p.y};	if (p.dirX == "right"){		if (p.x + polen_bounds.w / 2 < area_bounds.w){			p.x += speed;		} else {			p.dirX = "left"		}	} else {				if (p.x - polen_bounds.w / 2 > 0){			p.x -= speed;		} else {			p.dirX = "right";		}	}	if (p.dirY == "btm"){		if (p.y + polen_bounds.h / 2 < area_bounds.h){			p.y += speed;		} else {			p.dirY = "top"		}	} else {				if (p.y - polen_bounds.h / 2 > 0){			p.y -= speed;		} else {			p.dirY = "btm";		}	}}