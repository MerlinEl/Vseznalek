//http://jsfiddle.net/lannymcnie/kah9of6e/var stage = new createjs.Stage("canvas");createjs.Ticker.on("tick", stage);// Draw a circlevar c = new createjs.Shape().set({x:100, y:100});c.graphics.f("#f00").dc(0,0,25);// Draw a cross-hairvar x = new createjs.Shape().set({x:200, y:50});x.graphics.ss(3).s("#00f").mt(-10,0).lt(10,0).mt(0,-10).lt(0,10);// Add a hitArea to the cross-hair so it is easier to pressx.hitArea = new createjs.Shape(new createjs.Graphics().f("green").dc(0,0,10));// Add both to the stagestage.addChild(c, x);// Set the offset when the circle is pressedc.on("mousedown", function(e) {	c.offset = new createjs.Point(x.x-c.x, x.y-c.y);});// Add drag and drop to each shapec.on("pressmove", handleDrag);x.on("pressmove", handleDrag);function handleDrag(e) {	// Move the target to the mouse	e.target.x = e.stageX; e.target.y = e.stageY;    // If the target is the circle, also move the cross-hair  if (e.target == c) {    // Move the cross-hair    x.x = c.x + c.offset.x;    x.y = c.y + c.offset.y;  }}