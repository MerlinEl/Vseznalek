var audioPath = "https://playpen.createjs.com/CORS/";var sounds = [{  id: "mySound",  src: "creep.mp3"}];function init() {  createjs.Sound.addEventListener("fileload", handleLoad, true);  createjs.Sound.registerSounds(sounds, audioPath);}function handleLoad(event) {	document.body.addEventListener("click", function() {  	createjs.Sound.play("mySound");  }, { once: true });}init();// ORvar scripts = [    "lesson_begins.mp3",    "correct.mp3",    "wrong.mp3",    "fix.mp3"]scripts.forEach(n => loadAudio(n));/** * Play audio * @example   * var audio = document.getElementById("correct"); * if (audio) audio.play(); */function loadAudio(fname) {    var head = document.getElementsByTagName('head')[0];    var el = document.createElement('audio');    el.type = 'audio/mpeg';    el.async = true;    el.src = '/_common/levelCommon/assets/audio/' + fname;    el.id = fname.replace(/\.[^\/.]+$/, ''); // fname without extension    head.appendChild(el);}