 var cards_text = "";icomp.slots.forEach((s) => {	if (s.cardInSlot) cards_text += s.cardInSlot.text;});// OR single line             var cards_text  = icomp.slots.map((s)=>{return s.cardInSlot ? s.cardInSlot.text : ""}).join('');//OR more optimized var cards_text  = icomp.slots.map((s)=>{return s.cardInSlot?.text }).join('');// Or filter and pick (wee will get array of ids)var appIds = objs.filter(function(o) {    return o.selected;}).map(function(o) { return o.id; });// OR  filter only secret textFileds and pick text from firstvar letter = this.text_fields_list.filter((tf) => tf.isSecret)[0]?.text;// ORvar letter = this.text_fields_list.find((tf) => tf.isSecret)?.text;