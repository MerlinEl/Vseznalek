    get cardsCount() { return Object.keys(this.cards).length; }    getCardAtIndex(index) { return Object.values(this.cards)[index] }    getCardByName(name) { return this.cards[name] }var obj = { first: 'someVal' };obj[Object.keys(obj)[0]]; //returns 'someVal'//ORObject.values(example)[0]//ORfor (var prop in object) {    // object[prop]    break;}const result = Object.entries(obj).map(([key, value]) => {  console.log(key); // 👉️ name, country  console.log(value); // 👉️ James, Chile  return {[key]: value};});