//https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Regular_ExpressionsstrHasBackslashes = (/\\/).test(str);strHasForwardslashes = new RegExp("/","").test(str);Regex to accept alphanumeric and some special character.		tf.addEventListener("keyup", inputCallback.bind(this));		function inputCallback(evt) {			var str = evt.target.value;			// restrict other tahan > numbers, comma and minus -23.05			str = filterSymbols(str);			// add space between numbers 1 000, 10 000, 100 000, ...			str = addSpaces(str);			evt.target.value = str;		}		function filterSymbols(str) {			var regex = /[^0-9/,-]/g;			return str.replace(regex, "");		}		function addSpaces(str) {			var parts = str.toString().split(".");			var regex = /\B(?=(\d{3})+(?!\d))/g;			parts[0] = parts[0].replace(regex, " ");			return parts.join(".");		}	}