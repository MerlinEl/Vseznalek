<script>Klavesy.nastavitAkce(        {               // Esc            27 : function() {                alert("ESC");            },            // Left            37 : function() {                alert("Left");            },            // Right            39 : function() {                alert("Right");            },            // Alt            18 : function() {                alert("Alt");            },            // Shift            16 : function() {                alert("Shift");            }            /*            ,            // Up            38 : function() {            },            // Down            40 : function() {            },            // F5            116 : function() {            },                       // Enter            13 : function() {            },            // Space            32 : function() {            },            // Backspace            8 : function() {            },            // Delete            46 : function() {            }            */        }    );</script>var Klavesy = function() {	var akce = [];    var drzKod = null;    var klavesaDrz = function(e) {        var event = window.event || e;        var kod = event.keyCode;            var target = event.srcElement || event.target;        if(target.tagName == "BUTTON" || target.tagName == "INPUT" ||         target.tagName == "TEXTAREA" || target.tagName == "SELECT") return;                if (akce[kod]) {        	drzKod = kod;        	return false;        }    };    var klavesaPust = function(e) {        var event = window.event || e;        var kod = event.keyCode;            var target = event.srcElement || event.target;        if(target.tagName == "BUTTON" || target.tagName == "INPUT" ||         target.tagName == "TEXTAREA" || target.tagName == "SELECT") return;                if (akce[kod]) {        	if (drzKod && drzKod != kod) {        	    akce[drzKod]();        	    drzKod = null;        	}        	akce[kod]();        	return false;        }    };    var nastavitAkce = function(objektAkci) {        document.onkeydown = klavesaDrz;        document.onkeyup = klavesaPust;    	akce = objektAkci;    };        return {        nastavitAkce : nastavitAkce    };}();