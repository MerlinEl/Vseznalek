//http://jsfiddle.net/lannymcnie/nJKHk/var stage = new createjs.Stage("canvas");createjs.Ticker.addEventListener("tick", tick);// Create the Shapevar shape = new createjs.Shape().set({x:100, y:100});shape.graphics.s("#f00").ss(1);stage.addChild(shape);// Draw Random Segmentsvar thisArc, angle = 0;while (angle <= 360) {    var thisArc = Math.random() * 20 + 20 | 0; // Between 20 & 40 degrees    var startAngle = angle * Math.PI/180;    var endAngle = Math.min(360, angle+thisArc-5)*Math.PI/180;    shape.graphics.moveTo(0,0)    shape.graphics.arc(0,0,50,startAngle,endAngle);    //shape.graphics.lt(0,0); // This could close each arc.        angle += thisArc;}function tick(event) {    stage.update();}//OR//http://jsfiddle.net/lannymcnie/nJKHk/1/var stage = new createjs.Stage("canvas");createjs.Ticker.addEventListener("tick", tick);// Create the Shapevar shape = new createjs.Shape().set({x:100, y:100});stage.addChild(shape);// Draw Random Segmentsvar thisArc, angle = 0;while (angle <= 360) {    var thisArc = Math.random() * 20 + 20 | 0; // Between 20 & 40 degrees    var startAngle = angle * Math.PI/180;    var endAngle = Math.min(360, angle+thisArc)*Math.PI/180;        shape.graphics.f(createjs.Graphics.getRGB(Math.random()*0xFFFFFF));    shape.graphics.moveTo(0,0)    shape.graphics.arc(0,0,50,startAngle,endAngle);    //shape.graphics.lt(0,0); // This could close each arc.        angle += thisArc;}function tick(event) {    stage.update();}