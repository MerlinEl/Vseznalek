// create blur effectvar blurFilter = new createjs.BlurFilter(5, 5, 10);// animate blur		createjs.Tween		.get(blurFilter, {loop:true})		.to({ blurX: 0, blurY: 0}, 2000)		.wait(500)		.to({ blurX: 5, blurY: 5}, 2000)// add blur			c.filters = [colorFilter, blurFilter];			updateFilterInRect(c, new Rectangle2D(50, 50, 100, 100));//update blursetInterval(function(){	allCards.forEach(c=>{		updateFilterInRect(c, new Rectangle2D(50, 50, 100, 100));	});}, 400);// remove blur			c.filters = [];			updateFilterInRect(c, new Rectangle2D());		});function updateFilterInRect(ct, offsetRect){		var bounds = ct.nominalBounds;		ct.cache(			-bounds.width / 2 - offsetRect.x, 			-bounds.height / 2 - offsetRect.y, 			bounds.width + offsetRect.w, 			bounds.height + offsetRect.h		);}createjs.Ticker.addEventListener("tick", handleTick);function handleTick(event) { 	card.update();}