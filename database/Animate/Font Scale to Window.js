window.addEventListener("resize", scaleFont);	function scaleFont() {		/*log(			"resize:", tf, 			"fontSize", tf.style.fontSize,			"width start:", tl.startSize.w, canvas.width		)*/		/*log(			"resize:", tf, 			"fontSize", tf.style.fontSize,			"width start:", tl.startSize.w, canvas.width		)*/		var scale_percent = (canvas.width / Edhance.documentSize.w) * 100;		tf.style.fontSize = ((opt.fontSize / 100) * scale_percent) + "px";		/*log(			"TO:", ((opt.fontSize / 100) * scale_percent) + "px", 			"RESULT", tf.style.fontSize,		)*/	}	scaleFont();setTimeout(function(){		var tf = document.getElementById('tf_input_f0_01');	tf.type = "number";	console.log(tf);	tf.setFont = function (font) {		var size = this.offsetWidth,		font_size = size * font;		console.log("size:", size, "font_size:", font_size)		this.style.fontSize = font_size + '%';		return this;	};	  	tf.setFont(2.0);	  	window.onresize = function () {		tf.setFont(2.0);	}	}, 0);