// BY LABELvar _stage = this; // stagevar _maxPage = _stage.timeline.duration - 1; // total pagesvar _frame = 0;// event handlersfunction navPrev() {	if (_stage.timeline.position > 0) {		_frame--;		_stage.gotoAndStop('slide_'+_frame);	}}function navNext() {	if (_stage.timeline.position < _maxPage) {		_frame++;		_stage.gotoAndStop('slide_'+_frame);	}}// BY INDEXvar _stage = this; // stagevar _maxPage = _stage.timeline.duration - 1; // total pages// event handlersfunction navPrev() {	var cur = _stage.timeline.position;	if (cur > 0) {		_stage.gotoAndStop(cur - 1);	}}function navNext() {	var cur = _stage.timeline.position;	if (cur < _maxPage) {		_stage.gotoAndStop(cur + 1);	}}