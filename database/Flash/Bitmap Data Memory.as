import flash.system.*;	System.gc();var kb:Number = System.totalMemory / 1024;var share:Boolean = bitmaps[0].bitmapData == bitmaps[1].bitmapData;memUsage.text = "Memory Usage: " + kb + " KB. Sharing? " + share;			var size_a:int = bdata_a.getPixels(bdata_a.rect).length;			var size_b:int = bdata_b.getPixels(bdata_b.rect).length;			trace( "Bitmap Data size a:"+size_a+" b:"+size_b )