			//convert text in to XML string			var NEW_XML_DATA = "<BUBBLE>\n";			for each (var d:Object in TXTData) {								var btext = d.text;				btext = rhombusSwitch(btext, true) //replace # --> ♦				btext = btext.split("\r").join("\n");				var itm = '\t<' + d.label + ' value="' + btext + '"/>\n';				NEW_XML_DATA += itm;			}			NEW_XML_DATA += "</BUBBLE>";			XML_DATA = new XML(NEW_XML_DATA);XML.prettyPrinting = true; //collapse all lines in to one