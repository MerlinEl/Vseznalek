//In this tutorial will show how to convert an image to black and white. //First import the image of your choice and convert it to a movie clip and give it an instance name of “image_mc”.import flash.filters.ColorMatrixFilter;import fl.motion.AdjustColor;var color : AdjustColor;var colorMatrix : ColorMatrixFilter;var matrix : Array;var filterBW : Array;//create a new AdjustColor and set its color properties in order to create a black an white effect.//the idea is to desaturate the image.color = new AdjustColor();color.brightness = 20;color.contrast = 20;color.hue = 0;color.saturation = -100;//generate the flat array of values for all 4 color properties by calling the CalculateFinalFlatArray() method.matrix = color.CalculateFinalFlatArray();//use this generated array to create a ColorMatrixFilter and set the filterBW to an array containing this ColorMatrixFilter.colorMatrix = new ColorMatrixFilter(matrix);filterBW = [colorMatrix];//apply the filterBW to the movie clip named "image_mc"image_mc.filters = filterBW;