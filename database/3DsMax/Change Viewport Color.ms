CurrentColor = (GetUIColor 41*255) as colorif CurrentColor != color 0 0 0 then(SetUIColor 41 (color 0 0 0)colorMan.repaintUI #repaintAll)else(SetUIColor 41 (color 131 131 131)colorMan.repaintUI #repaintAll))The viewport settings (gradient/solid) are per scene, not global, therefore the only way to have your preference kept is to set it up in an empty and save it as Maxstart.max. More on Maxstart can be found in the help.SetUIColor 41 (color 200 200 200)colorMan.repaintUI #repaintAllchange also gradient for viewport