--remove last word from selected objectslocal last_word = "_Vlasta"for o in $selection do o.name = (substring o.name 1 (o.name.count - last_word.count))for o in objects where findString  o.name  "jana" != undefined do (		local pos = findString  o.name  "jana"	local prefix = substring o.name 1 (pos-2)	o.name = prefix)for o in selection do (		o.name += "_Vlasta")export_abc_path = "C:\\temp\\ee\\best.abc"fn setBackslashes str =( 	local cnt = str.count	for i = 1 to cnt do(		if str[i] == "\\" do str = replace str i 1 "/"	)	print str      return str)setBackslashes export_abc_path-- "C:/temp/ee/best.abc"