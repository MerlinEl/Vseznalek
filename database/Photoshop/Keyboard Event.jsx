if (ScriptUI.environment.keyboardState.shiftKey){} else {}Clear Horizontal or Vertical GuIt can be done with a script....#target photoshop;if(documents.length){var count = app.activeDocument.guides.length;for(var a =count -1;a>=0;a--){if (ScriptUI.environment.keyboardState.shiftKey){if(app.activeDocument.guides[a].direction==Direction.VERTICAL) app.activeDocument.guides[a].remove();}else{if(app.activeDocument.guides[a].direction==Direction.HORIZONTAL) app.activeDocument.guides[a].remove();    }}};If you just run the script all Horizontal guides are removed, if you run the script with the shift key pressed Vertical guides are removed.ides//Script - Is there way how read if CTRL key is pressed down while script is running?This might be what you are after...//app.notifiersEnabled = true;//app.notifiers.add( "setd",new File('~/desktop/align.jsx'), "Prpr" );try {if (arguments.length >= 2) {CTRL = ScriptUI.environment.keyboardState.ctrlKey;var desc = arguments[0];if(desc.hasKey(stringIDToTypeID('to'))){switch(typeIDToStringID(desc.getObjectValue(stringIDToTypeID('to')).getEnumerationValue(stringIDToTypeID('align')))){    case 'left' : leftFunction(); break;    case 'center' : centerFunction(); break;    case 'right' : rightFunction(); break;    default : break;    }}}function leftFunction(){    alert("left - Control key pessed = " + CTRL);    }function rightFunction(){    alert("right - Control key pessed = " + CTRL);    }function centerFunction(){    alert("center - Control key pessed = " + CTRL);    }}catch(e){alert(e + "\n" + e.line);}