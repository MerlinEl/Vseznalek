		wordAxis = createWordAxis();			wordAxis.rotateY(90);			console.log("wordAxis:", wordAxis);function createWordAxis() {			var pos = new THREE.Vector3(0, 0, 0);			var dir_y = new THREE.Vector3(0, 1, 0);			var arrowY = new THREE.ArrowHelper(dir_y, pos, 100, 0x00ff00);			var dir_x = dir_y.clone();			var axis = new THREE.Vector3(0, 0, 1);			var deg = -90;			var angle = deg * (Math.PI / 180);			dir_x.applyAxisAngle(axis, angle);			var arrowX = new THREE.ArrowHelper(dir_x, pos, 100, 0xff0000);			axis = new THREE.Vector3(0, 1, 0);			var dir_z = dir_x.clone();			dir_z.applyAxisAngle(axis, angle);			var arrowZ = new THREE.ArrowHelper(dir_z, pos, 100, 0x0000ff);			scene.add(arrowX);			scene.add(arrowY);			scene.add(arrowZ);			return {				arrowX: { mesh: arrowX, dir: dir_x },				arrowY: { mesh: arrowY, dir: dir_y },				arrowZ: { mesh: arrowZ, dir: dir_z },				// todo - not finished				rotateX(degrees) {					let rad = THREE.MathUtils.degToRad(degrees);					this.arrowX.dir.applyAxisAngle(new THREE.Vector3(0, 0, 1), rad);					this.arrowX.mesh.lookAt(this.arrowX.dir);				},				// todo - not finished				rotateY(degrees) {					let rad = THREE.MathUtils.degToRad(degrees);					this.arrowY.dir.applyAxisAngle(new THREE.Vector3(0, 0, 1), rad);					this.arrowY.mesh.lookAt(this.arrowY.dir);				},				// todo - not finished				rotateZ(degrees) {					let rad = THREE.MathUtils.degToRad(degrees);					this.arrowZ.dir.applyAxisAngle(new THREE.Vector3(0, 1, 0), rad);					this.arrowZ.mesh.lookAt(this.arrowZ.dir);				}			};		}