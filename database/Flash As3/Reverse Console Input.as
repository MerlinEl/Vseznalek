import fl.controls.TextArea;import orien.tools.*;var _ta:TextAreavar _text:String = "I dont want to skip white spaces. I am trying to delete new line special character\n\in text area, but it doesn't delete from text area. But displaying correctly,\n\in debugging mode only we can see this.\n\And imp point here is i have added changeHandler() for textArea ,\n\when i delete any character it's fired but when i delete new line\n\character it doesn't fired."_ta.text = _text;function write(str:String, ... args):void {		if (args.length > 0) str = mcString.substitute(str, args);	var console_text:String = _ta.text;	if (_ta.textField.numLines > 0){				var first_line:String = _ta.textField.getLineText(0);		first_line = first_line.replace("\r", "")		trace("first_line:{0}", first_line)		console_text = console_text.substr(first_line.length);		ctrace("console_text:{0}", console_text)	}	_ta.text = (first_line + str + console_text);}function writeLine(str:String, ... args):void {	if (args.length > 0) str = mcString.substitute(str, args);	_ta.text = str + "\n" + _ta.text;}writeLine("abc")writeLine("def")write("xbc:")write("1649")writeLine("dsdbg;hj d[]dgdg")