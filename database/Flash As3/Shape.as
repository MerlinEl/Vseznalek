import fl.text.TLFTextField;import flash.display.Shape;import flash.utils.getQualifiedClassName;import flash.text.TextField;var trgt = examples_01.target_01; // get movieclip with shapes and texts insidectrace("trgt:{0}", trgt.numChildren)for (var i:int = 0; i < trgt.numChildren; i++) {    	var child:* = trgt.getChildAt(i);	//ctrace("child:{0}", getQualifiedClassName(child)); //TCMText Shape	//if( child is Shape ){	if( child is TextField ){				//child.hasOwnProperty('name')		//ctrace("name:{0}", child.name)		/*if (child.name.indexOf("instance") == -1) {			ctrace("name:{0}", child)			for (var k in child){				ctrace("key:{0} val:{1}", k, child[k])			}		}*/		        var tf:TextField = child as TextField;		ctrace("text:{0}", tf);		ctrace("text:{0}", tf.text);		tf.alpha = 0.1;				/*for (var k in sh){			ctrace("key:{0} val:{1}", k, sh[k])		}		ctrace("shape:{0}", sh.graphics);*/    }}