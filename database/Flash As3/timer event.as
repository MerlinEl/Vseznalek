import flash.utils.getTimer;package  {     import flash.display.Sprite;     import flash.events.TimerEvent;     import flash.utils.Timer;      public class ShortTimer extends Sprite     {         public function ShortTimer()          {             // creates a new five-second Timer             var minuteTimer:Timer = new Timer(1000, 5);                          // designates listeners for the interval and completion events             minuteTimer.addEventListener(TimerEvent.TIMER, onTick);             minuteTimer.addEventListener(TimerEvent.TIMER_COMPLETE, onTimerComplete);                          // starts the timer ticking             minuteTimer.start();         }          private function onTick(event:TimerEvent):void  {             // displays the tick count so far             // The target of this event is the Timer instance itself.             trace("tick " + event.target.currentCount);         }         private function onTimerComplete(event:TimerEvent):void {             trace("Time's Up!");         }     } }tick 1 tick 2 tick 3 tick 4 tick 5 Time's Up!sleep(100)		private function sleep(sleep_time:Number):void{					var start_time = getTimer();			while (true) {			if (getTimer() - start_time >= sleep_time) break;			}		};function sleep(counter: int, subsequentFunction: Function, args: Array): void{    if (counter > 0)        callLater(sleep, [counter - 1, subsequentFunction, args]);    else        callLater(subsequentFunction, args);}// call trace('Hello') after 100 cyclessleep(100, trace, ['Hello']);// call myFunction() after 50 cyclessleep(50, myFunction, []);timer.reset();timer.start();import flash.utils.Timer;import flash.events.TimerEvent;var bubble_timer:Timer = new Timer(2000,1)		public function showDealyedMessage(msg:String, delay:Number):void{			//TweenLite.delayedcall(3, displayMessage);			bubble_timer.stop();			bubble_instance.bubble_text.text = msg;			bubble_timer.removeEventListener(TimerEvent.TIMER, showMessage);			if (delay != -1) {				bubble_timer.delay = delay*1000; //2*1000=2sec				bubble_timer.addEventListener(TimerEvent.TIMER, showMessage);				bubble_timer.start();			}			function showMessage(event:TimerEvent):void{				bubble_instance.bubble_text.text = getScore(msg);				bubble_timer.removeEventListener(TimerEvent.TIMER, showMessage);			}		};		´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´import flash.utils.setTimeout;import flash.utils.clearInterval;delay = setInterval(wait, 3000)		function wait():void{trace("delay end."); clearInterval(delay);}´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´				import flash.utils.setTimeout;//wait 1 second and executesetTimeout(function ():void { myGreeter.kocourkuUdelej("huraa"); }, 1000);´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´´var realCount:int = Math.floor(timer.currentCount/realDelay);