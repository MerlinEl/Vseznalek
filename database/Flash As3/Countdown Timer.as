		private function getRemainTime():String {						var today:Date = new Date();			var currentYear = yoday.getFullYear();			//getTime() > An integer expressing the number of milliseconds between the time of date and midnight, January 1, 1970. 			//Positive if after January 1, 1970; negative if before.			var currentTime = today.getTime();			var targetDate:Date = new Date(currentYear, 01, 21);			var targetTime = targetDate.getTime();			var timeLeft = targetTime - currentTime;			var sec = Math.floor(timeLeft/1000);			var min = Math.floor(sec/60);			var hours = Math.floor(min/60);			var days = Math.floor(hours/24);			sec = String(sec % 60);			if (sec.lenght < 2){								sec = "0" + sec;			}			min = String(min % 60);			if (min.lenght < 2){								min = "0" + min;			}			hours = String(hours % 24);			if (hours.lenght < 2){								hours = "0" + hours;			}			days = String(days);			if (timeLeft > 0){								var counter:String = days + ":" + hours + ":" + min + ":" + sec;				watch.text=counter;				watchGlow.text=counter;			} else {								trace("TIME'S UP");				var newTime:String = "00:00:00:00";				watch.text=newTime;				watchGlow.text=newTime;			}		}//testvar today:Date = new Date();var currentYear = today.getFullYear();var currentTime = today.getTime();var targetDate:Date = new Date(currentYear, 01, 21);var targetTime = targetDate.getTime();var timeLeft = targetTime - currentTime;ctrace("today:{0}\ntargetDate:{1}", today, targetDate)ctrace("currentTime:{0} targetTime:{1} timeLeft:{2}", currentTime, targetTime, timeLeft)targetTime = currentTime + 2*60*60*1000; //hours to min to sec to milisectimeLeft = targetTime - currentTime;ctrace("today:{0}\ntargetDate:{1}", today, targetDate)ctrace("currentTime:{0} targetTime:{1} timeLeft:{2}", currentTime, targetTime, timeLeft)