			if (all_ranges.length < 1) generateRanges();						var exam:Object = all_ranges.pop();			while (mcMath.isSympathetic(exam.numerator, exam.denominator)) {								if (all_ranges.length < 1) generateRanges();				exam = all_ranges.pop();			}			_multiplicator.text = exam.multiplicator;			_numerator.text = exam.numerator;			_denominator.text = exam.denominator;						if (exam.multiplicator > exam.denominator) {								_cage_01.tag = exam.multiplicator / exam.denominator;				_cage_02.tag = exam.denominator / exam.denominator;							} else {								_cage_01.tag = exam.multiplicator / exam.multiplicator;				_cage_02.tag = exam.denominator / exam.multiplicator;			}						_card_01.text = "0";			_card_01.result = mcMath.roundToDecimals(exam.multiplicator * exam.numerator / exam.denominator, 2);